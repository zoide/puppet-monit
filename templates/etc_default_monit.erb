##   PUPPET MANAGED   ###

# /etc/default/monit

# Defaults for monit initscript.  This file is sourced by
# /bin/sh from /etc/init.d/monit.

# You must set this variable to yes for monit to start
START=<%= startup %>

# Options to pass to monit
MONIT_OPTS='-s /var/lib/monit/state'
